services:
  - type: web
    name: reimbursement-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mock_reimbursement_api:app
    autoDeploy: true
    branch: main
